# Display Diagnostics

## Intent

There is no way to capture display timing data from a live session. When investigating rendering artefacts, the developer must modify the source, rebuild, and rerun — a slow loop that also risks altering the behaviour under observation.
