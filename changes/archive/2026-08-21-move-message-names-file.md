# Move Message Names File

**Mode:** Wander

## Intent

The message logged after a file move says only where it went (`Moved to /…/electronic/2`), not what moved. Scanning the log later, the operator can't tell which track was filed. Name the file in the message, as the name-clash error already does.

## Conclusion

Completed. One-line message change in the move outcome; 0.29.17.
